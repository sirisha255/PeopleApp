@model IEnumerable<City>
@*@model List<City>*@
@{
    ViewData["Title"] = "City Index";
}

<h1>City list</h1>
<a asp-action="Create" class="btn btn-primary mt-4 mb-4">Add new city</a>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th></th>
            @*<th>City</th>*@
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            @*<th>Country</th>*@
            <th>@Html.DisplayNameFor(model => model.CityName)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                @*<td>@item.CityId</td>
            <td>@item.CityName</td>
            <td>@item.Country.CountryName</td>*@
                <td>@Html.DisplayFor(modelItem=> item.Id)</td>
                <td>@Html.DisplayFor(modelItem => item.CityName)</td>
                <td>@Html.DisplayFor(modelItem => item.Country.CountryName)</td>
                <td>
                    @*<a asp-action="Edit" asp-controller="City" asp-route-id="@modelTtem.CityId" class="mr-4 text-warning">Edit</a>
                <a asp-action="Delete" asp-controller="City" asp-route-id="@city.CityId" class="mr-4 text-danger">Delete </a>*@
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id })|
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })|
                </td>


            </tr>
        }
    </tbody>
</table>